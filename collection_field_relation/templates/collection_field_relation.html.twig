{#
/**
 * @file collection_field_relation.html.twig
 * Default theme implementation to present Collection field relation data.
 *
 * This template is used when viewing Collection field relation pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_collection_field_relation()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('collection_field_relation') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
